server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.16.12.116:4000/uat_message?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: uat_message
    password: '!Dusto123'
  redis:
    host: 10.0.4.141
    port: 7000
    database: 0
    timeout: 10000
  rabbitmq:
    host: 10.0.4.156
    port: 5672
    username: mq
    password: mq
    virtual-host: /
    publisher-confirm-type: correlated
    publisher-returns: true
    template:
      mandatory: true
  # Elasticsearch 配置
  elasticsearch:
    uris: http://10.0.4.156:9200
  data:
    elasticsearch:
      repositories:
        enabled: true

  # 国际化配置
  messages:
    basename: i18n/messages
    encoding: UTF-8
    cache-seconds: 3600
    fallback-to-system-locale: false
    default-locale: zh_CN
    # 默认语言
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.system.shop.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000 # 24 hours


logging:
  level:
    root: info
    com:
      system:
        shop: debug
    org:
      springframework:
        security: DEBUG
        web: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n'

