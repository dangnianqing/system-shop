<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.shop.mapper.SysRoleMenuMapper">
    <resultMap id="roleMenuMap" type="com.system.shop.entity.SysRoleMenu">
        <id property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="menuId" column="menu_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleteFlag" column="delete_flag"/>
    </resultMap>

    <select id="findByRoleId" resultMap="roleMenuMap">
        SELECT * FROM sys_role_menu
        WHERE role_id = #{roleId}
        AND delete_flag = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role_menu (
            role_id, menu_id, create_time, update_time, delete_flag
        ) VALUES (
            #{roleId}, #{menuId}, NOW(), NOW(), 0
        )
    </insert>

    <update id="deleteByRoleId">
        UPDATE sys_role_menu
        SET delete_flag = 1,
            update_time = NOW()
        WHERE role_id = #{roleId}
    </update>

    <update id="deleteByMenuId">
        UPDATE sys_role_menu
        SET delete_flag = 1,
            update_time = NOW()
        WHERE menu_id = #{menuId}
    </update>
</mapper> 