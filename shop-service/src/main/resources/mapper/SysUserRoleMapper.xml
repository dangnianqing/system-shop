<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.shop.mapper.SysUserRoleMapper">
    <resultMap id="userRoleMap" type="com.system.shop.entity.SysUserRole">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleteFlag" column="delete_flag"/>
    </resultMap>

    <select id="findByUserId" resultMap="userRoleMap">
        SELECT * FROM sys_user_role
        WHERE user_id = #{userId}
        AND delete_flag = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user_role (
            user_id, role_id, create_time, update_time, delete_flag
        ) VALUES (
            #{userId}, #{roleId}, NOW(), NOW(), 0
        )
    </insert>

    <update id="deleteByUserId">
        UPDATE sys_user_role
        SET delete_flag = 1,
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <update id="deleteByRoleId">
        UPDATE sys_user_role
        SET delete_flag = 1,
            update_time = NOW()
        WHERE role_id = #{roleId}
    </update>
</mapper> 